2026-03-28 — Sitzung: Kafka Channel / Memory-Webhook

Kurzzusammenfassung der Aktionen heute:

- Initialer Eintrag: Kafka Streams App / EventStore-Pattern als zentrales Konzept für die Masterarbeit vermerkt (MEMORY.md).
- Memory-Update + Webhook: MEMORY.md wurde aktualisiert und eine Benachrichtigung an #general getestet.
- Implementiert: scripts/write_memory.sh — atomarer Writer, automatisches Commit & Push, Webhook-Notify.
  - Atomic write (tmpfile -> mv).
  - No-change check (keine Notification standardmäßig, optional per NOTIFY_ON_NOCHANGE).
  - Support für beliebige Ziel-Dateien (nicht nur MEMORY.md).
  - changed_files in Webhook-Payload.
  - Commit-Message mit Dateinamen und Timestamp.
  - Commit-SHA und commit_url in Payload.
  - Optionaler asynchroner Push (ASYNC_PUSH=1) mit Hintergrund-Notifier (scripts/push_notify_bg.sh).
- Git/GitHub: Repo initialisiert lokal, SSH-Deploy-Key erzeugt und als Deploy Key auf GitHub registriert.
  - Branch-Namen vereinheitlicht (master -> main). Remote master gelöscht; main ist jetzt remote.
  - Alle Änderungen wurden per SSH-Push auf https://github.com/kevo3000/openCommerceClaw.git gepusht.
- Incoming Webhook: Payload-Format angepasst (text, commit_url, changed_files). Tests durchgeführt — geprüft via lokaler Receiver und (versuchter) POST an die angegebene Webhook-URL.
- Extras: scripts/push_notify_bg.sh (Background-Pusher) und Hook-Installer-Primer entwickelt.

Nächste Schritte / Empfehlungen:
- README + scripts/install-hook.sh ins Repo, damit Mitwirkende Hook & Deploy-Key leicht installieren.
- (Optional) ASYNC_PUSH als Default für grosse Änderungen; oder Batch/Squash-Strategie für viele kleine automatic commits.
- Rocket.Chat: prüfe Incoming Webhook Deliveries im Server-UI für endgültige Sichtbarkeit in #general.

— Ende des Logs —
