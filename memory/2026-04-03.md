2026-04-03 — Install/Sandbox session log

- Started Confluent/Kafka/Schema Registry + Debezium setup in workspace kafka-confluent.
- Performed system checks (docker, docker-compose, disk, RAM, ports).
- Pulled multiple Confluent and Debezium images; managed slow network by switching strategies when needed.
- Created docker-compose.yml with zookeeper, kafka, schema-registry, and connect (initially debezium/connect).
- Encountered port collisions on 8083/8084 caused by local services; remapped Connect to 8084 then to 18083, then to 18083/18083 and finally 18083.
- Set up persistent docker volumes: cp_zookeeper_data, cp_kafka_data, cp_schema_registry_data, cp_connect_plugins.
- Built a custom local/debezium-connect-avro image to add Confluent Avro jars; ran into missing AvroConverter class issues.
- Changed approach: switching Connect to official Confluent Connect image and mounting plugin volume.
- Database (Broadleaf) specifics: host=localhost:5432, user=postgres, password=280103, DB=postgres (confirmed default). Allowed automatic changes.
- Set wal_level to logical via ALTER SYSTEM; user restarted DB service; verified wal_level logical.
- Created replication role 'debezium' and logical replication slot 'broadleaf_slot'.
- Ongoing: installing Confluent Avro converter plugin into Connect plugin path and finalising Debezium connector deployment.

Notes / next actions to persist:
- Finish installing Avro plugin into cp_connect_plugins and start confluentinc/cp-kafka-connect.
- Register Debezium Postgres connector and validate CDC events (test consumption from Kafka topic).
- Document connector JSON and SQL used for slot/user creation.

Context: work performed autonomously in the workspace; user authorized all actions and restarts. 
